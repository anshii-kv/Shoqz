<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Order Successful - Thank You!</title>
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" />
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
        <style>
            body {
                background: linear-gradient(135deg, #bddbeb 0%, #a8d0e6 100%);
                min-height: 100vh;
                font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            }

            .success-container {
                min-height: 100vh;
                display: flex;
                align-items: center;
                justify-content: center;
                padding: 2rem 0;
            }

            .success-card {
                background: white;
                border-radius: 20px;
                box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
                padding: 3rem;
                text-align: center;
                max-width: 600px;
                width: 100%;
                margin: 0 20px;
                position: relative;
                overflow: hidden;
            }

            .success-card::before {
                content: '';
                position: absolute;
                top: -50%;
                left: -50%;
                width: 200%;
                height: 200%;
                background: radial-gradient(circle, rgba(13, 110, 253, 0.05) 0%, transparent 70%);
                animation: pulse 4s ease-in-out infinite;
            }

            @keyframes pulse {
                0%, 100% { transform: scale(1); opacity: 0.5; }
                50% { transform: scale(1.1); opacity: 0.8; }
            }

            .success-icon {
                width: 120px;
                height: 120px;
                background: linear-gradient(135deg, #28a745, #20c997);
                border-radius: 50%;
                display: flex;
                align-items: center;
                justify-content: center;
                margin: 0 auto 2rem;
                position: relative;
                z-index: 1;
                animation: bounceIn 1s ease-out;
            }

            .success-icon i {
                font-size: 3rem;
                color: white;
                animation: checkmark 0.6s ease-in-out 0.9s both;
            }

            @keyframes bounceIn {
                0% { transform: scale(0.3); opacity: 0; }
                50% { transform: scale(1.05); }
                70% { transform: scale(0.9); }
                100% { transform: scale(1); opacity: 1; }
            }

            @keyframes checkmark {
                0% { transform: scale(0); opacity: 0; }
                100% { transform: scale(1); opacity: 1; }
            }

            .success-title {
                color: #2c3e50;
                font-size: 2.5rem;
                font-weight: 700;
                margin-bottom: 1rem;
                position: relative;
                z-index: 1;
                animation: fadeInUp 0.8s ease-out 0.3s both;
            }

            .success-subtitle {
                color: #6c757d;
                font-size: 1.2rem;
                margin-bottom: 2rem;
                position: relative;
                z-index: 1;
                animation: fadeInUp 0.8s ease-out 0.5s both;
            }

            .order-info {
                background: #f8f9fa;
                border-radius: 15px;
                padding: 2rem;
                margin: 2rem 0;
                border: 1px solid #e9ecef;
                position: relative;
                z-index: 1;
                animation: fadeInUp 0.8s ease-out 0.7s both;
            }

            .order-info h5 {
                color: #2c3e50;
                font-weight: 600;
                margin-bottom: 1rem;
            }

            .order-detail {
                display: flex;
                justify-content: space-between;
                align-items: center;
                margin-bottom: 0.5rem;
                padding: 0.5rem 0;
                border-bottom: 1px solid #e9ecef;
            }

            .order-detail:last-child {
                border-bottom: none;
                margin-bottom: 0;
            }

            .order-detail span:first-child {
                color: #6c757d;
                font-weight: 500;
            }

            .order-detail span:last-child {
                color: #2c3e50;
                font-weight: 600;
            }

            .btn-container {
                display: flex;
                gap: 1rem;
                justify-content: center;
                flex-wrap: wrap;
                margin-top: 2rem;
                position: relative;
                z-index: 1;
                animation: fadeInUp 0.8s ease-out 0.9s both;
            }

            .btn-custom {
                padding: 0.8rem 2rem;
                border-radius: 10px;
                font-weight: 600;
                text-decoration: none;
                transition: all 0.3s ease;
                border: none;
                cursor: pointer;
                position: relative;
                overflow: hidden;
                min-width: 180px;
            }

            .btn-primary-custom {
                background: linear-gradient(135deg, #0d6efd, #0b5ed7);
                color: white;
                box-shadow: 0 4px 15px rgba(13, 110, 253, 0.3);
            }

            .btn-primary-custom:hover {
                transform: translateY(-2px);
                box-shadow: 0 6px 20px rgba(13, 110, 253, 0.4);
                color: white;
            }

            .btn-outline-custom {
                background: white;
                color: #0d6efd;
                border: 2px solid #0d6efd;
            }

            .btn-outline-custom:hover {
                background: #0d6efd;
                color: white;
                transform: translateY(-2px);
                box-shadow: 0 4px 15px rgba(13, 110, 253, 0.3);
            }

            .celebration-animation {
                position: absolute;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                pointer-events: none;
                overflow: hidden;
                z-index: 0;
            }

            .confetti {
                position: absolute;
                width: 10px;
                height: 10px;
                background: #0d6efd;
                animation: confetti-fall 3s linear infinite;
            }

            .confetti:nth-child(1) { left: 10%; animation-delay: 0s; background: #28a745; }
            .confetti:nth-child(2) { left: 20%; animation-delay: 0.5s; background: #ffc107; }
            .confetti:nth-child(3) { left: 30%; animation-delay: 1s; background: #dc3545; }
            .confetti:nth-child(4) { left: 40%; animation-delay: 1.5s; background: #17a2b8; }
            .confetti:nth-child(5) { left: 50%; animation-delay: 2s; background: #6610f2; }
            .confetti:nth-child(6) { left: 60%; animation-delay: 2.5s; background: #fd7e14; }
            .confetti:nth-child(7) { left: 70%; animation-delay: 3s; background: #20c997; }
            .confetti:nth-child(8) { left: 80%; animation-delay: 3.5s; background: #e83e8c; }
            .confetti:nth-child(9) { left: 90%; animation-delay: 4s; background: #28a745; }

            @keyframes confetti-fall {
                0% { transform: translateY(-100vh) rotate(0deg); opacity: 1; }
                100% { transform: translateY(100vh) rotate(360deg); opacity: 0; }
            }

            @keyframes fadeInUp {
                from { opacity: 0; transform: translateY(30px); }
                to { opacity: 1; transform: translateY(0); }
            }

            .delivery-info {
                background: linear-gradient(135deg, #e3f2fd, #f0f8ff);
                border-radius: 15px;
                padding: 1.5rem;
                margin-top: 2rem;
                border: 1px solid #bbdefb;
                position: relative;
                z-index: 1;
                animation: fadeInUp 0.8s ease-out 1.1s both;
            }

            .delivery-info .delivery-icon {
                color: #0d6efd;
                font-size: 1.5rem;
                margin-right: 1rem;
            }

            .social-share {
                margin-top: 2rem;
                position: relative;
                z-index: 1;
                animation: fadeInUp 0.8s ease-out 1.3s both;
            }

            .social-share h6 {
                color: #6c757d;
                margin-bottom: 1rem;
            }

            .social-btn {
                display: inline-flex;
                align-items: center;
                justify-content: center;
                width: 40px;
                height: 40px;
                border-radius: 50%;
                margin: 0 0.5rem;
                text-decoration: none;
                color: white;
                transition: all 0.3s ease;
            }

            .social-btn:hover {
                transform: translateY(-2px);
                box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            }

            .social-btn.facebook { background: #3b5998; }
            .social-btn.twitter { background: #1da1f2; }
            .social-btn.whatsapp { background: #25d366; }
            .social-btn.instagram { background: #e4405f; }

            @media (max-width: 768px) {
                .success-card {
                    margin: 0 10px;
                    padding: 2rem;
                }
                
                .success-title {
                    font-size: 2rem;
                }
                
                .btn-container {
                    flex-direction: column;
                    align-items: center;
                }
                
                .btn-custom {
                    width: 100%;
                    max-width: 300px;
                }
            }
        </style>
    </head>
    <body>
        <div class="success-container">
            <div class="success-card">
                <!-- Celebration Animation -->
                <div class="celebration-animation">
                    <div class="confetti"></div>
                    <div class="confetti"></div>
                    <div class="confetti"></div>
                    <div class="confetti"></div>
                    <div class="confetti"></div>
                    <div class="confetti"></div>
                    <div class="confetti"></div>
                    <div class="confetti"></div>
                    <div class="confetti"></div>
                </div>

                <!-- Success Icon -->
                <div class="success-icon">
                    <i class="fas fa-check"></i>
                </div>

                <!-- Success Message -->
                <h1 class="success-title">Order Successful!</h1>
                <p class="success-subtitle">
                    Thank you for your purchase! Your order has been placed successfully and is now being processed.
                </p>

                <!-- Order Information -->
                <div class="order-info">
                    <h5><i class="fas fa-receipt me-2"></i>Order Details</h5>
                    <div class="order-detail">
                        <span>Order ID:</span>
                        <span id="orderId"><%= order.displayOrderId %></span>

                    </div>
                    <div class="order-detail">
                        <span>Order Date:</span>
                       <span id="orderDate"><%= new Date(order.createdAt).toLocaleDateString('en-IN', { year: 'numeric', month: 'long', day: 'numeric' }) %></span>

                    </div>
                    <div class="order-detail">
                        <span>Total Amount:</span>
                        <span id="totalAmount">₹<%= order.subtotal.toFixed(2) %></span>

                    </div>
                    <div class="order-detail">
                        <span>Payment Method:</span>
                      <span id="paymentMethod"><%= order.paymentMethod %></span>
                    </div>
                </div>

                <!-- Delivery Information -->
                <div class="delivery-info">
                    <div class="d-flex align-items-center">
                        <i class="fas fa-truck delivery-icon"></i>
                        <div class="text-start">
                            <h6 class="mb-1">Estimated Delivery</h6>
                            <p class="mb-0 text-muted">Your order will be delivered within <strong>5-7 business days</strong></p>
                        </div>
                    </div>
                </div>

                <!-- Action Buttons -->
                <div class="btn-container">
                    <a href="/order-details" class="btn-custom btn-primary-custom">
                        <i class="fas fa-file-invoice me-2"></i>
                        View Order Details
                    </a>
                    <a href="/shop" class="btn-custom btn-outline-custom">
                        <i class="fas fa-shopping-bag me-2"></i>
                        Continue Shopping
                    </a>
                </div>

                <!-- Social Share -->
                <div class="social-share">
                    <h6>Share your joy with friends!</h6>
                    <div>
                        <a href="#" class="social-btn facebook" title="Share on Facebook">
                            <i class="fab fa-facebook-f"></i>
                        </a>
                        <a href="#" class="social-btn twitter" title="Share on Twitter">
                            <i class="fab fa-twitter"></i>
                        </a>
                        <a href="#" class="social-btn whatsapp" title="Share on WhatsApp">
                            <i class="fab fa-whatsapp"></i>
                        </a>
                        <a href="#" class="social-btn instagram" title="Share on Instagram">
                            <i class="fab fa-instagram"></i>
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <script>
            // Animate elements on page load
            document.addEventListener('DOMContentLoaded', function() {
                // Set current date
                const today = new Date();
                const options = { year: 'numeric', month: 'long', day: 'numeric' };
                document.getElementById('orderDate').textContent = today.toLocaleDateString('en-IN', options);

                // Generate random order ID
                const randomId = Math.floor(Math.random() * 900000) + 100000;
                const orderId = "<%= order._id %>";
                const productId = "<%= order.product[0].productId%>"
                // Add click handlers for buttons
                document.querySelector('.btn-primary-custom').addEventListener('click', function(e) {
                    e.preventDefault();
                    // Add loading state
                    this.innerHTML = '<i class="fas fa-spinner fa-spin me-2"></i>Loading...';
                    
                    // Simulate redirect delay
                    setTimeout(() => {
                        window.location.href = `/order-details/${orderId}/${productId}`;
                    }, 1000);
                });

                document.querySelector('.btn-outline-custom').addEventListener('click', function(e) {
                    e.preventDefault();
                    // Add loading state
                    this.innerHTML = '<i class="fas fa-spinner fa-spin me-2"></i>Loading...';
                    
                    // Simulate redirect delay
                    setTimeout(() => {
                        window.location.href = '/shop';
                    }, 1000);
                });

                // Add click handlers for social sharing
                document.querySelectorAll('.social-btn').forEach(btn => {
                    btn.addEventListener('click', function(e) {
                        e.preventDefault();
                        
                        const platform = this.classList.contains('facebook') ? 'Facebook' :
                                       this.classList.contains('twitter') ? 'Twitter' :
                                       this.classList.contains('whatsapp') ? 'WhatsApp' : 'Instagram';
                        
                        // Show toast notification
                        showToast(`Opening ${platform}...`, 'info');
                        
                        // Here you would implement actual social sharing
                        // For now, just simulate the action
                        setTimeout(() => {
                            showToast(`Shared on ${platform}!`, 'success');
                        }, 1000);
                    });
                });

                // Create additional celebration effects
                createFloatingElements();
            });

            // Toast notification function
            function showToast(message, type = 'info') {
                const toast = document.createElement('div');
                toast.className = `alert alert-${type} alert-dismissible fade show`;
                toast.style.cssText = `
                    position: fixed;
                    top: 20px;
                    right: 20px;
                    z-index: 9999;
                    min-width: 300px;
                    animation: slideInRight 0.3s ease;
                `;
                
                toast.innerHTML = `
                    ${message}
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                `;
                
                document.body.appendChild(toast);
                
                setTimeout(() => {
                    toast.remove();
                }, 3000);
            }

            // Create floating celebration elements
            function createFloatingElements() {
                const container = document.querySelector('.celebration-animation');
                
                // Create hearts
                for (let i = 0; i < 5; i++) {
                    setTimeout(() => {
                        const heart = document.createElement('div');
                        heart.innerHTML = '❤️';
                        heart.style.cssText = `
                            position: absolute;
                            font-size: 20px;
                            left: ${Math.random() * 100}%;
                            top: 100%;
                            animation: floatUp 4s ease-out;
                            pointer-events: none;
                        `;
                        container.appendChild(heart);
                        
                        setTimeout(() => heart.remove(), 4000);
                    }, i * 800);
                }
                
                // Add float animation keyframes
                const style = document.createElement('style');
                style.textContent = `
                    @keyframes floatUp {
                        0% { transform: translateY(0) rotate(0deg); opacity: 1; }
                        100% { transform: translateY(-100vh) rotate(360deg); opacity: 0; }
                    }
                    
                    @keyframes slideInRight {
                        from { transform: translateX(100%); opacity: 0; }
                        to { transform: translateX(0); opacity: 1; }
                    }
                `;
                document.head.appendChild(style);
            }

            // Add some interactive feedback
            document.querySelectorAll('.btn-custom').forEach(btn => {
                btn.addEventListener('mouseenter', function() {
                    this.style.transform = 'translateY(-2px) scale(1.02)';
                });
                
                btn.addEventListener('mouseleave', function() {
                    this.style.transform = 'translateY(0) scale(1)';
                });
            });
        </script>
        
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    </body>
</html>