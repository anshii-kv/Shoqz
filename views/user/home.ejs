<!-- <%-include("../../views/partials/user/header")%> -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Footwear Collection</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', 'Arial', sans-serif;
            line-height: 1.6;
            color: #2c3e50;
            background-color: #f8f9fa;
            margin: 0;
            padding: 0;
        }

        .colorlib-intro {
            padding: 100px 0;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            position: relative;
            overflow: hidden;
        }

        .colorlib-intro::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="grain" width="100" height="100" patternUnits="userSpaceOnUse"><circle cx="25" cy="25" r="1" fill="%23fff" opacity="0.1"/><circle cx="75" cy="75" r="0.8" fill="%23fff" opacity="0.08"/><circle cx="50" cy="10" r="1.2" fill="%23fff" opacity="0.12"/></pattern></defs><rect width="100" height="100" fill="url(%23grain)"/></svg>');
            pointer-events: none;
        }

        .intro {
            font-size: 3.2rem;
            font-weight: 300;
            color: white;
            max-width: 900px;
            margin: 0 auto;
            text-shadow: 0 4px 8px rgba(0,0,0,0.3);
            animation: fadeInUp 1s ease-out;
            text-align: center;
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .colorlib-product {
            padding: 100px 0;
            background: white;
        }

        .featured {
            position: relative;
            overflow: hidden;
            border-radius: 25px;
            box-shadow: 0 20px 50px rgba(0,0,0,0.12);
            transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            margin-bottom: 40px;
            height: 600px;
            cursor: pointer;
        }

        .featured:hover {
            transform: translateY(-12px) scale(1.02);
            box-shadow: 0 30px 70px rgba(0,0,0,0.2);
        }

        .featured-img {
            display: block;
            height: 100%;
            background-size: cover;
            background-position: center;
            transition: all 0.6s ease;
            position: relative;
        }

        .featured-img::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(45deg, rgba(102, 126, 234, 0.4), rgba(118, 75, 162, 0.3));
            transition: all 0.4s ease;
        }

        .featured:hover .featured-img::before {
            background: linear-gradient(45deg, rgba(102, 126, 234, 0.2), rgba(118, 75, 162, 0.15));
        }

        .featured .desc {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            padding: 50px;
            background: linear-gradient(to top, rgba(0,0,0,0.8), rgba(0,0,0,0.4), transparent);
            transform: translateY(20px);
            transition: all 0.4s ease;
        }

        .featured:hover .desc {
            transform: translateY(0);
        }

        .featured .desc h2 {
            color: white;
            font-size: 2.5rem;
            font-weight: 700;
            margin: 0;
            text-shadow: 0 2px 8px rgba(0,0,0,0.6);
            letter-spacing: 1px;
        }

        .featured .desc h2 a {
            color: white;
            text-decoration: none;
            transition: all 0.3s ease;
            position: relative;
        }

        .featured .desc h2 a::after {
            content: '';
            position: absolute;
            bottom: -8px;
            left: 0;
            width: 0;
            height: 4px;
            background: #ffd700;
            transition: width 0.3s ease;
            border-radius: 2px;
        }

        .featured .desc h2 a:hover::after {
            width: 100%;
        }

        .colorlib-heading {
            margin-bottom: 80px;
        }

        .colorlib-heading h2 {
            font-size: 4rem;
            font-weight: 800;
            color: #2c3e50;
            margin-bottom: 20px;
            position: relative;
            display: inline-block;
            text-transform: uppercase;
            letter-spacing: 3px;
        }

        .colorlib-heading h2::after {
            content: '';
            position: absolute;
            bottom: -20px;
            left: 50%;
            transform: translateX(-50%);
            width: 100px;
            height: 5px;
            background: linear-gradient(45deg, #667eea, #764ba2);
            border-radius: 3px;
        }

        /* Enhanced Product Card Styles */
        .product-card {
            background: white;
            border-radius: 25px;
            overflow: hidden;
            transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            box-shadow: 0 10px 35px rgba(0,0,0,0.08);
            border: 2px solid transparent;
            height: 100%;
            display: flex;
            flex-direction: column;
            position: relative;
        }

        .product-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            border-radius: 25px;
            padding: 2px;
            background: linear-gradient(45deg, transparent, rgba(102, 126, 234, 0.2), transparent);
            mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
            -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
            mask-composite: exclude;
            -webkit-mask-composite: destination-out;
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .product-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 25px 50px rgba(0,0,0,0.15);
        }

        .product-card:hover::before {
            opacity: 1;
        }

        .product-image-wrapper {
            position: relative;
            overflow: hidden;
            height: 320px;
            flex-shrink: 0;
            border-radius: 25px 25px 0 0;
        }

        .product-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: all 0.6s ease;
        }

        .product-card:hover .product-image {
            transform: scale(1.1);
        }

        .discount-badge {
            position: absolute;
            top: 20px;
            right: 20px;
            background: linear-gradient(45deg, #ff6b6b, #ee5a24);
            color: white;
            padding: 12px 18px;
            border-radius: 30px;
            font-weight: 700;
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            box-shadow: 0 6px 20px rgba(255, 107, 107, 0.4);
            z-index: 10;
        }

        .product-overlay {
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(45deg, rgba(102, 126, 234, 0.85), rgba(118, 75, 162, 0.85));
            display: flex;
            align-items: center;
            justify-content: center;
            opacity: 0;
            transition: all 0.4s ease;
            backdrop-filter: blur(2px);
        }

        .product-overlay i {
            font-size: 2.5rem;
            color: white;
            transform: scale(0.5);
            transition: all 0.4s ease;
        }

        .product-card:hover .product-overlay {
            opacity: 1;
        }

        .product-card:hover .product-overlay i {
            transform: scale(1);
        }

        .product-info {
            padding: 35px 25px;
            text-align: center;
            flex-grow: 1;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        .product-title {
            font-size: 1.3rem;
            font-weight: 600;
            margin-bottom: 20px;
            color: #2c3e50;
            line-height: 1.4;
            min-height: 3.5em;
        }

        .product-title a {
            color: #2c3e50;
            text-decoration: none;
            transition: all 0.3s ease;
        }

        .product-title a:hover {
            color: #667eea;
        }

        .price-section {
            margin-bottom: 25px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 15px;
        }

        .current-price {
            font-size: 1.6rem;
            font-weight: 800;
            background: linear-gradient(45deg, #667eea, #764ba2);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .original-price {
            font-size: 1.2rem;
            color: #999;
            text-decoration: line-through;
            font-weight: 500;
        }

        .product-actions {
            margin-top: auto;
        }

        .btn-cart {
            background: linear-gradient(45deg, #667eea, #764ba2);
            border: none;
            color: white;
            padding: 15px 30px;
            border-radius: 30px;
            text-decoration: none;
            font-weight: 600;
            font-size: 1rem;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 10px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            width: 100%;
            justify-content: center;
        }

        .btn-cart:hover {
            background: linear-gradient(45deg, #5a67d8, #6b46c1);
            transform: translateY(-3px);
            box-shadow: 0 10px 25px rgba(102, 126, 234, 0.4);
            color: white;
        }

        .btn-primary {
            background: linear-gradient(45deg, #667eea, #764ba2);
            border: none;
            padding: 20px 50px;
            font-size: 1.3rem;
            font-weight: 700;
            border-radius: 50px;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 2px;
            box-shadow: 0 10px 30px rgba(102, 126, 234, 0.3);
        }

        .btn-primary:hover {
            transform: translateY(-4px);
            box-shadow: 0 15px 40px rgba(102, 126, 234, 0.5);
            background: linear-gradient(45deg, #5a67d8, #6b46c1);
        }

        /* Improved Responsive Design */
        @media (min-width: 1400px) {
            .col-product {
                flex: 0 0 auto;
                width: 20%; /* 5 products per row on very large screens */
            }
        }

        @media (min-width: 1200px) and (max-width: 1399px) {
            .col-product {
                flex: 0 0 auto;
                width: 25%; /* 4 products per row on large screens */
            }
        }

        @media (min-width: 992px) and (max-width: 1199px) {
            .col-product {
                flex: 0 0 auto;
                width: 33.333333%; /* 3 products per row on medium screens */
            }
        }

        @media (min-width: 768px) and (max-width: 991px) {
            .col-product {
                flex: 0 0 auto;
                width: 50%; /* 2 products per row on small tablets */
            }
        }

        @media (max-width: 767px) {
            .col-product {
                flex: 0 0 auto;
                width: 100%; /* 1 product per row on mobile */
                max-width: 400px;
                margin: 0 auto;
            }
            
            .intro {
                font-size: 2.2rem;
                padding: 0 20px;
            }
            
            .colorlib-heading h2 {
                font-size: 2.8rem;
            }
            
            .featured {
                margin-bottom: 30px;
                height: 400px;
            }
            
            .product-image-wrapper {
                height: 250px;
            }
            
            .featured .desc {
                padding: 30px;
            }
            
            .featured .desc h2 {
                font-size: 1.8rem;
            }
            
            .colorlib-product {
                padding: 60px 0;
            }
        }

        .loading-animation {
            opacity: 0;
            animation: fadeIn 0.8s ease-out forwards;
        }

        @keyframes fadeIn {
            to {
                opacity: 1;
            }
        }

        /* Grid Layout Fix */
        .row {
            margin-left: -15px;
            margin-right: -15px;
        }

        .row > [class*="col-"] {
            padding-left: 15px;
            padding-right: 15px;
            margin-bottom: 40px;
        }

        /* Featured section background */
        .featured-collections {
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
        }

        /* Best sellers section styling */
        .best-sellers-section {
            background: linear-gradient(135deg, #ffffff 0%, #f8f9fa 100%);
        }
    </style>
</head>
<body>
    
    <!-- Featured Collections -->
    <div class="colorlib-product featured-collections">
        <div class="container-fluid px-4">
            <div class="row">
                <div class="col-lg-6 mb-4">
                    <div class="featured loading-animation">
                        <a href="#" class="featured-img" style="background-image: url('https://images.unsplash.com/photo-1595950653106-6c9ebd614d3a?w=1200&q=80');"></a>
                        <div class="desc">
                            <h2><a href="#">Men's Premium Collection</a></h2>
                        </div>
                    </div>
                </div>
                <div class="col-lg-6 mb-4">
                    <div class="featured loading-animation">
                        <a href="#" class="featured-img" style="background-image: url('https://images.unsplash.com/photo-1560769629-975ec94e6a86?w=1200&q=80');"></a>
                        <div class="desc">
                            <h2><a href="#">Women's Elegant Collection</a></h2>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Best Sellers Section -->
     <!-- Best Sellers Section -->
    <div class="colorlib-product" style="background: #f8f9fa;">
        <div class="container">
            <div class="row">
                <div class="col-12 text-center colorlib-heading">
                    <h2>Best Sellers</h2>
                </div>
            </div>
            
            <!-- Fixed Product Grid with Proper Responsive Classes -->
            <div class="row">
              <% products.forEach(product => { %>
                <div class="col-xl-3 col-lg-4 col-md-6 col-sm-6 col-12 mb-4 col-product">
                  <div class="product-card loading-animation">
                    <div class="product-image-wrapper">
                      <a href="/product/<%= product._id %>" class="product-link">
                        <img src="/uploads/product-images/<%= product.productImage[0] %>" class="product-image" alt="<%= product.productName %>">
                        <% if (product.productOffer > 0 || product.categoryoffer > 0) { %>
                          <div class="discount-badge">
                            <% if (product.productOffer > 0 && product.categoryoffer > 0) { %>
                              <%= Math.max(product.productOffer, product.categoryoffer) %>% OFF
                            <% } else if (product.productOffer > 0) { %>
                              <%= product.productOffer %>% OFF
                            <% } else { %>
                              <%= product.categoryoffer %>% OFF
                            <% } %>
                          </div>
                        <% } %>
                        <div class="product-overlay">
                          <i class="fas fa-eye"></i>
                        </div>
                      </a>
                    </div>
                    <div class="product-info">
                      <h3 class="product-title">
                        <a href="/product/<%= product._id %>"><%= product.productName %></a>
                      </h3>
                      <div class="price-section">
                        <span class="current-price">₹<%= product.finalamount %></span>
                        <% if (product.productOffer > 0 || product.categoryoffer > 0) { %>
                          <span class="original-price">₹<%= product.salePrice.toFixed(2) %></span>
                        <% } %>
                      </div>
                      <div class="product-actions">
                        <a href="/product/<%= product._id %>" class="btn btn-cart">
                          <i class="fas fa-eye"></i> View Product
                        </a>
                      </div>
                    </div>
                  </div>
                </div>
              <% }) %>
            </div>

            <div class="row mt-5">
                <div class="col-12 text-center">
                    <a href="#" class="btn btn-primary btn-lg">Shop All Products</a>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
    <script>
        // Staggered animation for elements
        document.addEventListener('DOMContentLoaded', function() {
            const elements = document.querySelectorAll('.loading-animation');
            elements.forEach((el, index) => {
                if (!el.style.animationDelay) {
                    el.style.animationDelay = `${index * 0.1}s`;
                }
            });

            // Smooth scrolling for internal links
            document.querySelectorAll('a[href^="#"]').forEach(anchor => {
                anchor.addEventListener('click', function (e) {
                    e.preventDefault();
                    const target = document.querySelector(this.getAttribute('href'));
                    if (target) {
                        target.scrollIntoView({
                            behavior: 'smooth',
                            block: 'start'
                        });
                    }
                });
            });
        });
    </script>
</body>
</html>

<!-- <%-include("../../views/partials/user/footer")%> -->